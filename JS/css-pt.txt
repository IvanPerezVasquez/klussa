
/* ======================================================
   LOGIN MOODLE – TEMA MOOVE (CSS ÚNICO)
   Diseño centrado | Moderno | Responsive
   ====================================================== */

/* 🎨 Variables institucionales */
:root{
  --primary:#0d6efd;
  --dark:#212529;
  --light:#f8f9fa;
  --accent:#20c997;
}

/* 🧱 Fondo completo del login */
#page-login-index{
  background: linear-gradient(135deg, var(--dark), #000);
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
}

/* 🧩 Contenedor principal */
#page-login-index .card{
  width:100%;
  max-width:420px;
  border:0;
  border-radius:18px;
  box-shadow:0 20px 40px rgba(0,0,0,.35);
  padding:2rem;
}

/* 🏷️ Logo */
#page-login-index .login-logo img{
  max-width:160px;
  margin:0 auto 1.5rem;
  display:block;
}

/* ✍️ Inputs */
#page-login-index input[type="text"],
#page-login-index input[type="password"]{
  border-radius:50px;
  padding:.6rem 1rem;
  border:1px solid #dee2e6;
}

#page-login-index input:focus{
  border-color:var(--primary);
  box-shadow:0 0 0 .2rem rgba(13,110,253,.25);
}

/* 🔐 Botón */
#page-login-index button[type="submit"]{
  width:100%;
  border-radius:50px;
  padding:.6rem;
  background:var(--primary);
  border:none;
  font-weight:600;
}

#page-login-index button[type="submit"]:hover{
  background:#0b5ed7;
}

/* 🔗 Enlaces */
#page-login-index a{
  color:var(--primary);
  font-weight:500;
}

#page-login-index a:hover{
  color:var(--accent);
}

/* 🧼 Ocultar elementos innecesarios */
#page-login-index .login-divider,
#page-login-index .cookies-policy,
#page-login-index .login-identityproviders{
  display:none;
}

/* 📱 Responsive */
@media (max-width:576px){
  #page-login-index .card{
    padding:1.5rem;
    border-radius:14px;
  }
}

/* ======================================================
   FIN LOGIN MOODLE – MOOVE
   ====================================================== */
